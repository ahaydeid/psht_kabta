# Components

Reusable frontend components are grouped by purpose.

- `ui/`: primitive interface components such as button, input, select, textarea, checkbox, badge, alert, and card.
- `Base/`: larger reusable blocks such as modal and table.

The default visual theme uses yellow, black/white, and red.
